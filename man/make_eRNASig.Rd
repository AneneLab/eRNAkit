% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eDecosus.R
\name{make_eRNASig}
\alias{make_eRNASig}
\title{Make eRNA based signature for fine cell typoe deconvolution.}
\usage{
make_eRNASig(db = eRNAkitDB, type = "cell", specificity = 2, cpm = 20, n = 10)
}
\arguments{
\item{type}{Character; one of \code{"cell"} or \code{"organ"} specifying which
signature type to filter.}

\item{specificity}{Numeric; minimum Specificity Score to retain rows
(default: 2).}

\item{cpm}{Numeric; minimum CPM (counts per million) to retain rows
(default: 20).}

\item{n}{Integer; number of top rows to return per group after filtering and sorting
(default: 10).}

\item{eRNAkitDB}{A list containing at least the \code{EOC} data frame with
eRNA signatures and their metadata.}
}
\value{
A data frame containing filtered and combined top eRNA signatures
  grouped by the specified \code{type} (cell or organ).
}
\description{
This function creates the signature for eDecosus from an \code{eRNAkitDB} database object
based on the specified \code{type} ("cell" or "organ"), minimum specificity,
and CPM thresholds. It returns the top \code{n} entries per group (cell or organ)
by two ranking methods:
1. Specificity Score then CPM
2. CPM then Specificity Score
The results from both methods are combined and deduplicated.
}
\examples{
\dontrun{
eRNAkitDB <- readRDS("path/to/eRNAkitDB.rds")
eSig <- make_eRNASig(eRNAkitDB, type = "cell",
                                                 specificity = 2,
                                                 cpm = 20,
                                                 n = 10) }
}
